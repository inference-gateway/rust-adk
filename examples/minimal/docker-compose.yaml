---
services:
  server:
    build:
      context: ../..
      dockerfile: examples/Dockerfile.server
      args:
        BIN_NAME: minimal-server
        EXAMPLE_PATH: examples/minimal
    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - --silent
        - http://localhost:8081/health
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - a2a-network

  client:
    build:
      context: ../..
      dockerfile: examples/Dockerfile.client
      args:
        BIN_NAME: minimal-client
    depends_on:
      server:
        condition: service_healthy
    environment:
      SERVER_URL: http://server:8081
    networks:
      - a2a-network

networks:
  a2a-network:
    driver: bridge
